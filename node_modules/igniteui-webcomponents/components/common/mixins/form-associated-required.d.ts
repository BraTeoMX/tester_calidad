import type { LitElement } from 'lit';
import type { Constructor } from './constructor.js';
import { type FormAssociatedElementInterface } from './form-associated.js';
export declare class FormRequiredInterface {
    protected requiredChange(): void;
    /**
     * Makes the control a required field in a form context.
     * @attr
     */
    required: boolean;
}
/**
 * Turns the passed class element into a Form Associated Custom Element with
 * additional `required` attribute.
 */
export declare function FormAssociatedRequiredMixin<T extends Constructor<LitElement>>(superClass: T): Constructor<FormRequiredInterface & FormAssociatedElementInterface> & T;
